const useTaskListStyles = () => {
  const wrapperStyles = {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'space-between',
    alignItems: 'flex-start',
    marginLeft: '10px',
    padding: '10px',
    height: '100%',
  }

  return { wrapperStyles }
}

export default useTaskListStyles
